node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
cookie.txt
.env
# Local Netlify folder
.netlify

# Next.js build output
admin-ui/.next/
admin-ui/out/
admin-ui/node_modules/

# Lockfiles contain registry-specific URLs (e.g. Replit's internal proxy)
# Let each environment generate its own
package-lock.json
admin-ui/package-lock.json

# Analysis reports
